<div x-data="{ isOpen: false }" class="border-b border-gray-200 py-6">
  <h3 class="-my-3 flow-root">
    <!-- Expand/collapse section button -->
    <button type="button"
      class="flex w-full items-center justify-between bg-[#f0f6f4] py-3 text-sm text-gray-400 hover:text-gray-500"
      aria-controls="filter-section-0" :aria-expanded="isOpen ? 'true' : 'false'" x-on:click="isOpen = !isOpen">
      <span class="font-medium text-gray-900">
        <%= filterTitle %>
      </span>
      <span class="ml-6 flex items-center">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4 transform transition-transform" :class="{'rotate-180': isOpen}">
          <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5" />
        </svg>
      </span>
    </button>
  </h3>
  <!-- Filter section, show/hide based on section state. -->
  <div class="pt-6" id="filter-section-0" x-show.transition.opacity.duration.500ms="isOpen">
    <div class="space-y-4">
      <% filterType.forEach(filterType => { %>
        <%- include('../atoms/filter-desktop', filterType) %>
      <% }) %>
    </div>
  </div>
</div>
