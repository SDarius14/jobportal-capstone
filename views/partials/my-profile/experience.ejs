<%- include('../../components/organisms/profileForm-header', {header, description}) %>
  <div class="px-4 py-6 sm:p-8">
    <div class="max-w-2xl space-y-10">
      <!-- Experiences Foreach -->
      <% experiences.forEach(({ header, inputGroup, textareaLabel }) => { %> 
          <%- include('../../components/molecules/experience-fieldset', { header, inputGroup, textareaLabel }) %>
      <% }); %>

<%- include('../../components/organisms/profileForm-footer') %>

